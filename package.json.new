{
  "name": "dojo-to-react-monorepo",
  "version": "1.0.0",
  "description": "Monorepo for Dojo to React migration with single-spa adapters",
  "private": true,
  "scripts": {
    "clean": "rimraf **/dist",
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint"
  },
  "devDependencies": {
    "rimraf": "^3.0.2",
    "prettier": "^2.8.4",
    "eslint": "^8.36.0",
    "typescript": "^4.9.5"
  },
  "engines": {
    "node": ">=14",
    "pnpm": ">=7"
  }
} 